type Product = {
  id: number;          // 1‒30
  title: string;       // Product name
  category: string;    // e.g. "Shoes", "Electronics"
  price: number;       // Price in rupees
  rating: number;      // 0-5 rating (float)
  image: string;       // Primary image  "/images/1.jpg"
  description: string;
};
